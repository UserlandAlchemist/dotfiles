# Temporary and editor files
*~
*.swp
*.bak
.DS_Store

# Claude Code CLI directory (only settings.local.json is versioned)
.claude/
!.claude/settings.local.json

# LLM collaboration task files (ephemeral state, not permanent docs)
CODEX-TODO.txt
AGENTS.md
AGENTS-IDENTITY-POLICY.md

# Emacs-generated state and caches
*/.emacs.d/eln-cache/
*/.emacs.d/elpa/
*/.emacs.d/auto-save-list/
*/.emacs.d/url/
*/.emacs.d/bookmarks
*/.emacs.d/places
*/.emacs.d/recentf
*/.emacs.d/sessions/
*/.emacs.d/gnupg/

# Waybar, Sway, and mako runtime files
*/.config/waybar/*.bak
*/.config/waybar/*~
*/.config/sway/*~
*/.config/mako/*.pid

# Fonts and icons cache
*/.local/share/icons/hicolor/
*/.local/share/fonts/**/.uuid

# Local testing and temp binaries
*/.local/bin/test*
*/.local/bin/tmp*
*/.local/bin/uv
*/.local/bin/uvx
*/.local/bin/protontricks*

# --- Sensitive / host-local data (never commit) ---

# SSH keys and credentials
ssh-*/.ssh/id_*
ssh-*/.ssh/*_rsa
ssh-*/.ssh/*_dsa
ssh-*/.ssh/*_ecdsa
ssh-*/.ssh/*_ed25519
ssh-*/.ssh/known_hosts
ssh-*/.ssh/authorized_keys

# SSH keys in any location (catch accidental copies)
**/id_rsa
**/id_dsa
**/id_ecdsa
**/id_ed25519

# API tokens and secrets
**/*.token
**/*.secret
**/secrets.json
**/credentials.json
**/.env
**/.env.local
**/.env.*.local

# Borg encryption key and metadata (host-specific)
borg-user-*/.config/borg/passphrase
borg-user-*/.config/borg/security/

# PSD runtime state file (can include absolute paths)
*/.config/psd/.psd.conf

# --- Cache and build artifacts ---

# Python
**/__pycache__/
**/*.pyc
**/*.pyo
**/.pytest_cache/
**/.mypy_cache/
**/.ruff_cache/
**/*.egg-info/

# Node
**/node_modules/

# General caches
**/.cache/

# Build artifacts
**/build/
**/dist/

# Compiled files
**/*.o
**/*.so
**/*.dylib

# --- Session and lock files ---

# Vim
**/.netrwhist
**/Session.vim

# Lock files (may contain absolute paths)
**/*.lock
